import React from 'react';

// This component is no longer used and has been replaced by static HTML pages
// for each project. It can be safely deleted.
const ProjectDetailPage: React.FC = () => {
  return null;
};

export default ProjectDetailPage;
